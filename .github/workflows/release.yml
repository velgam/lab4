name: Deploy to GitHub Pages on Release

on:
  release:
    types: [published]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: 
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    permissions:
      contents: write
      pages: write
      id-token: write
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '18'

    - name: Create version deployment
      run: |
        VERSION_NAME="${GITHUB_REF#refs/tags/}"
        echo "🚀 Deploying version: $VERSION_NAME"
        
        # Создаем папку для новой версии
        mkdir -p "versions/$VERSION_NAME"
        
        # Копируем файлы в версионную папку
        cp -r en ru index.html "versions/$VERSION_NAME/"
        
        # Обновляем ссылки в HTML файлах для версионных страниц
        find "versions/$VERSION_NAME" -name "*.html" -exec sed -i "s|href=\"/LAB4/|href=\"/LAB4/versions/$VERSION_NAME/|g" {} \;
        find "versions/$VERSION_NAME" -name "*.html" -exec sed -i "s|href=\"ru/|href=\"/LAB4/versions/$VERSION_NAME/ru/|g" {} \;
        find "versions/$VERSION_NAME" -name "*.html" -exec sed -i "s|href=\"en/|href=\"/LAB4/versions/$VERSION_NAME/en/|g" {} \;
        
        # Добавляем информацию о версии на страницы
        find "versions/$VERSION_NAME" -name "*.html" -exec sed -i "s|Версия: current|Версия: $VERSION_NAME|g" {} \;
        find "versions/$VERSION_NAME" -name "*.html" -exec sed -i "s|Version: current|Version: $VERSION_NAME|g" {} \;

    - name: Update versions list
      run: |
        VERSION_NAME="${GITHUB_REF#refs/tags/}"
        echo "📝 Adding version to versions.json: $VERSION_NAME"
        
        # Создаем или читаем существующий versions.json
        if [ -f versions.json ]; then
          echo "📁 versions.json exists, updating..."
          node -e "
            const fs = require('fs');
            let versions = [];
            try {
              versions = JSON.parse(fs.readFileSync('versions.json'));
            } catch (e) {
              console.log('Creating new versions array');
            }
            
            // Добавляем новую версию если её нет
            if (!versions.find(v => v.name === '$VERSION_NAME')) {
              versions.unshift({
                name: '$VERSION_NAME',
                url: 'versions/$VERSION_NAME',
                date: new Date().toISOString(),
                description: 'Automated release'
              });
              console.log('✅ Added new version: $VERSION_NAME');
            } else {
              console.log('ℹ️ Version already exists: $VERSION_NAME');
            }
            
            // Сохраняем обновленный список
            fs.writeFileSync('versions.json', JSON.stringify(versions, null, 2));
            console.log('Total versions:', versions.length);
          "
        else
          echo "📁 Creating new versions.json..."
          echo '[{"name": "'$VERSION_NAME'", "url": "versions/'$VERSION_NAME'", "date": "'$(date -I)'", "description": "First release"}]' > versions.json
        fi
        
        echo "📄 Current versions.json:"
        cat versions.json

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: .

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4

    - name: Show deployment info
      run: |
        echo "✅ Successfully deployed version: ${GITHUB_REF#refs/tags/}"
        echo "🌐 Your site is available at: ${{ steps.deployment.outputs.page_url }}"